version : '2'
services:
  my-mysql:
    image: mysql:latest
    ports:
      - "3306:3306"
    environment:
      MYSQL_DATABASE: demo
      MYSQL_PASSWORD: dbp4ss
      MYSQL_USER: dbuser
      MYSQL_DATABASE: demo
      MYSQL_ROOT_PASSWORD: p4SSW0rd

      DEFAULT_AUTHENTICATION_PLUGIN: mysql_native_password
    container_name: mio-mysql
    volumes:
      - mio-mysqlvolume:/var/lib/mysql

  mio-app:
    image: smukk9/msandeep-io:1.0
    ports:
      - "8080:8080"
    links:
      - mio-msyql
    depends_on:
      - mio-mysql
    container_name: mio-app
volumes:
    mio-mysql-volume:
      external: true