version : '2'
services:
  mysqldbserver:
    image: smukk9/mysql-msandeep-io
    ports:
      - "3306:3306"
    environment:
      MYSQL_DATABASE: demo
      MYSQL_PASSWORD: dbp4ss
      MYSQL_USER: dbuser
      MYSQL_DATABASE: demo
      MYSQL_ROOT_PASSWORD: p4SSW0rd

      DEFAULT_AUTHENTICATION_PLUGIN: mysql_native_password
    container_name: mysqldbserver

  springappserver:
    image: smukk9/msandeep-io
    ports:
      - "8080:8080"
    links:
      - mysqldbserver
    depends_on:
      - mysqldbserver
